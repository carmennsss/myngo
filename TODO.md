# TODO - Arregla los tests de front

- [ ] Corregir imports y referencias en `frontend/myngo_app/test/widget/screens/auth_screens_test.dart` para usar `PantallaLogin` y `PantallaRegistro` (rutas correctas).
- [ ] Ajustar expectativas del widget para no depender de strings hardcodeados (Tolgee), verificando principalmente estructura (campos, etc.).
- [ ] Ejecutar `flutter test` para validar que los tests pasan.
- [ ] Si queda algún fallo, identificar el siguiente test roto y repetir.

