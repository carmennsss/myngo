/// Implementación vacía para Mobile que evita errores de compilación en Web.
/// La lógica real de mobile se delega a url_launcher en el archivo principal.
Future<void> descargarArchivoWeb(String url, String nombre) async {
  // No se hace nada aquí, se usa la rama else del kIsWeb en gestor_descargas.dart
}
